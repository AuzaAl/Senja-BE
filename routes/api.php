<?php

// Versioned API routes. The "api/v1" prefix is applied automatically via
// `apiPrefix: 'api/v1'` in bootstrap/app.php, so routes here must NOT
// re-add the "v1" segment.

require __DIR__.'/api/v1.php';
